version: '3'

services:
  postgres:
    image: 'postgres:14.3'
    restart: always
    ports:
      - '5432:5432'
    environment:
      - POSTGRES_USER=admin
      - POSTGRES_PASSWORD=Admin12345.
      - POSTGRES_DB=ventas